Method Assignment
This project demonstrates how to create a method with an optional parameter in C#.
The console app asks the user for one or two integers, then calls a method that performs a math operation and returns the result.
